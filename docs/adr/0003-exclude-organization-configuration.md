# Exclude organization configuration

The synchronization scope is personal software configuration only. Configuration tied to employers or organizations, including internal domains, registries, usernames, URL rewrites, network paths, and proprietary toolchains, is excluded even when it appears in otherwise accepted files such as shell or Git configuration.
