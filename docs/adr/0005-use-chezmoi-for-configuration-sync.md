# Use chezmoi for configuration sync

Use chezmoi as the configuration sync engine with the dedicated private GitHub repository as its source repository. Custom code is limited to discovery, user review, orchestration, and reporting, while chezmoi owns templating, diff preview, and applying accepted configuration to target devices.
