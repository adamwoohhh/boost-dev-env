# Resolve target conflicts interactively

When applying configuration to a target device, existing files at managed paths create Configuration Conflicts. The apply flow shows a diff and lets the user skip, replace with backup, or merge using an external merge tool rather than overwriting or merging silently.
