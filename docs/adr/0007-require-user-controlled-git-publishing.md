# Require user-controlled Git publishing

Capture writes accepted configuration changes to the local checkout of the private sync repository but does not commit or push them. The user reviews the working tree and performs Git commit and push manually, keeping publication of configuration changes under explicit human control.
