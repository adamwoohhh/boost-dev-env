# Exclude secrets from the first version

The first version synchronizes personal software configuration that can safely live in a private GitHub repository without secret encryption. Tokens, passwords, private keys, organization configuration, and other sensitive material are excluded by default; encrypted secret synchronization can be added later only for explicitly requested personal configuration.
